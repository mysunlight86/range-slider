
// import SliderBody from '../src/modules/sliderBody';
// import SliderThumb from '../src/modules/sliderThumb';

describe('Slider', () => {
  // it('can create slider line', () => {
  //   const line: SliderBody = new SliderBody();
  //   expect(line).not.toBe(null);
  // });

  // it('can create slider thumb', () => {
  //   const thumb: SliderThumb = new SliderThumb();
  //   expect(thumb).not.toBe(null);
  // });
});
